## PriceWise Frontend

React frontend for the PriceWise C2B pricing console.

The app expects the FastAPI backend to run at:

```text
http://127.0.0.1:8000
```

Set `VITE_API_BASE` to point to another backend URL.

```bash
pnpm install
pnpm dev
```
